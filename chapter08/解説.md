
```sh
$ g++ will-it-move.cpp -o p -std=c++20
$ ./p
It moved!
No, it is not move assignable!
```

`move()`はリソースの移動であり、値を変えないことを明示する`const`がついた変数にmoveが適用できてしまうとconstの意味が破綻する。したがってconstがついた変数は中身を変えないためにコピーが走ると考えると良い。

今回のコードはC言語寄りのC++の書き方。わざわざmoveを禁止するために書いたと思われるが、もしmoveを禁止したいなら moveをdeleteする書き方にしておけば良く、今回の問題のような書き方はしない。
```C++
WillItMove(WillItMove&&) = delete;
```


制約はクラスの境界(インタフェース)におき，内部の自由度は残す考え．
```C++
class WillItMove
{
public:
    WillItMove(WillItMove&&) = default;
    WillItMove(const WillItMove&) = default;
    WillItMove& operator=(WillItMove&&) = default;
    WillItMove& operator=(const WillItMove&) = default;

    const Member& member() const & { return member_; }//ここがポイント
    Member member() && { return std::move(member_); }

private:
    Member member_{};
};
```

理由
- ムーブコンストラクタ/代入が正しく生成され、実際にムーブされる
- 標準コンテナ・アルゴリズムで普通に使える
- 利用者から見た不変性は変わらない
- 将来「実は遅延初期化したい」「キャッシュを持ちたい」となっても、外部インタフェースを壊さずに実装を変えられる





# 参考

[lvalues, rvalues, glvalues, prvalues, xvalues, help!](https://blog.knatten.org/2018/03/09/lvalues-rvalues-glvalues-prvalues-xvalues-help/)


[値カテゴリ](https://ja.cppreference.com/cpp/language/value_category)


AI解答

> C++17 の保証されたコピー省略以降、prvalue は「一時オブジェクトそのもの」ではなく 結果オブジェクトを初期化するための式 と定義し直されました。オブジェクトが実体化するのは、参照束縛やメンバアクセスなどで temporary materialization conversion ([conv.rval]) が適用され xvalue に変換された時点です。そのため T x = f(); は「戻り値を作ってからコピー」ではなく、x が直接初期化されます。


[Guaranteed Copy Elision Does Not Elide Copies](https://tartanllama.xyz/posts/guaranteed-copy-elision/)

